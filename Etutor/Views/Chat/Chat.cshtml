@model Etutor.Models.Message
@{
    ViewBag.Title = "Chat";
}

<h2>Chat</h2>

@using(Html.BeginForm("SendMessage", "Chat", FormMethod.Post))
{
    <label>Message:</label> @Html.TextBoxFor(h => h.BodyText)
    <input type="submit" value="Send" />

}

